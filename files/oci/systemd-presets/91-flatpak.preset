# Default unit policy for Flatpak setup.
#
# flathub-remote-add.service is a oneshot that configures the Flathub
# remote (--if-not-exists, system-wide) at first network availability.
# Vendor-neutral: downstream distros that prefer a different remote
# (e.g. razorfin's own mirror, or Fedora's flatpak remote) can drop a
# 92-<thing>.preset that disables this and enables their alternative.

enable flathub-remote-add.service

# flatpak-preinstall.service runs `flatpak preinstall` (1.17+) to
# materialise every app declared by .preinstall files under
# /usr/share/flatpak/preinstall.d/ and /etc/flatpak/preinstall.d/.
# The subcommand is idempotent across boots and respects user
# uninstalls (it won't reinstall what the user explicitly removed).
# See cosmic-deps/flatpak.bst for why we vendor flatpak 1.17.6.
enable flatpak-preinstall.service

# flatpak-system-update.timer fires daily at 04:00 (with up to 1h
# jitter, Persistent= to catch up after sleep) and runs
# `flatpak update -y --noninteractive`. ExecCondition= on the service
# skips on metered NetworkManager connections so it doesn't burn
# tethered data. Mirrors ublue-os-update-services.
enable flatpak-system-update.timer
