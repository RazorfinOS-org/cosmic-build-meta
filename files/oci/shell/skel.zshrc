# Your personal zsh config. System-wide defaults live in /etc/zshrc
# (history, completion, prompt, aliases) and are applied before this file.
# Add your own options, aliases, and functions below.
#
# The presence of this file also stops zsh from launching the
# zsh-newuser-install first-run wizard on first login.
