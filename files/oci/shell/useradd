# Defaults for `useradd`. cosmic-initial-setup creates the primary account
# via accountsservice -> `useradd -m` (no -s), so SHELL= here sets the
# default login shell.
GROUP=100
HOME=/home
INACTIVE=-1
EXPIRE=
SHELL=/usr/bin/zsh
SKEL=/etc/skel
CREATE_MAIL_SPOOL=no
