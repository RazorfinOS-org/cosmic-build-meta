# Faster shutdown: give services 15 seconds to stop before SIGKILL,
# instead of the systemd default of 90s. The 90s ceiling is a UX
# disaster on desktops -- a single misbehaving service hangs the
# entire shutdown for a minute and a half. Bazzite and Pop ship 15s.
[Manager]
DefaultTimeoutStopSec=15s
