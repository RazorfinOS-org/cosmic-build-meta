# Enable NetworkManager's connectivity check so captive portals are
# detected (cosmic notifies + browsers offer the login page) and the
# network applet can distinguish "connected" from "online". GNOME's
# endpoint is operated for exactly this distro use case.
[connectivity]
uri=http://nmcheck.gnome.org/check_network_status.txt
