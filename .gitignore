# BuildStream
.bst/
builddir/
workspaces/
*.log

# Build artifacts
target/

# VM / image artifacts produced by `just build`, `just generate-bootable-image`,
# and `just boot-vm` (OCI checkout, sparse disk, OVMF NVRAM, debug socket, console log).
build/

# Python
__pycache__/
*.pyc

# Editor backups
*~
*.swp

# Dev-mode symlinks to sibling repos consumed by `kind: local`. Pair
# with the Justfile's EXTRA_MOUNTS hook to make them visible from the
# BST container. Currently unused (cosmonaut-installer is consumed via
# github:); kept as scaffolding for future sibling-repo development.
dev/
